import erp_scraper, requests
import logging

logging.basicConfig(level=logging.DEBUG)

def debug_login():
    session = requests.Session()
    # Mocking perform_login steps to see where it fails
    # ...
    pass

debug_login()
